publisher_forward_position_controller:
  ros__parameters:

    controller_name: "pm_robot_xyz_axis_FPC"
    wait_sec_between_publish: 10

    # Position for controller in sequential order

    goal_names: ["pos1", "pos2"]
    pos1: [0.0, 0.0, 0.0]
    pos2: [-0.15, 0.0, 0.0]
